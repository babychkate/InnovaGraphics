const initialState = {
    loading: false,
    success: false,
    errors: [],
};

export const answerReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};