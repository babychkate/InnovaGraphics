const initialState = {
    loading: false,
    success: false,
    errors: [],
};

export const questionReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};